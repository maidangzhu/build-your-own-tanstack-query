{
  "name": "the-tanstack-query",
  "version": "1.0.0",
  "description": "A comprehensive TanStack Query implementation workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "type-check": "pnpm -r type-check"
  },
  "keywords": [
    "react-query",
    "tanstack-query",
    "state-management",
    "data-fetching",
    "caching"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.8.1",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsup": "^8.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
